{
    "name": "my-inventory-app-root",
    "private": true,
    "scripts": {
        "test:e2e": "playwright test",
        "test:e2e:ui": "playwright test --ui",
        "test:e2e:headed": "playwright test --headed",
        "test:e2e:debug": "playwright test --debug",
        "test:e2e:no-server": "PLAYWRIGHT_SKIP_WEBSERVER=1 playwright test",
        "test:e2e:no-server:ui": "PLAYWRIGHT_SKIP_WEBSERVER=1 playwright test --ui",
        "test:e2e:no-server:headed": "PLAYWRIGHT_SKIP_WEBSERVER=1 playwright test --headed",
        "test:e2e:no-server:debug": "PLAYWRIGHT_SKIP_WEBSERVER=1 playwright test --debug"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.1"
    }
}
